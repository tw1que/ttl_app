diff --git a/ttl_server/tests/app.test.ts b/ttl_server/tests/app.test.ts
new file mode 100644
--- /dev/null
+++ b/ttl_server/tests/app.test.ts
@@ -0,0 +1,27 @@
+import { describe, it, expect } from 'vitest';
+import request from 'supertest';
+import app from '../src/app.js';
+
+describe('health', () => {
+  it('returns ok', async () => {
+    const res = await request(app).get('/health');
+    expect(res.status).toBe(200);
+    expect(res.body).toEqual({ ok: true });
+  });
+});
+
+describe('items/:id', () => {
+  it('400 on invalid id', async () => {
+    const res = await request(app).get('/items/not-a-uuid');
+    expect(res.status).toBe(400);
+  });
+});
diff --git a/ttl_server/vitest.config.ts b/ttl_server/vitest.config.ts
new file mode 100644
--- /dev/null
+++ b/ttl_server/vitest.config.ts
@@ -0,0 +1,19 @@
+import { defineConfig } from 'vitest/config';
+
+export default defineConfig({
+  test: {
+    environment: 'node',
+    coverage: {
+      provider: 'v8',
+      all: true,
+      include: ['src/**/*.ts'],
+      exclude: ['src/**/*.d.ts', 'src/**/__mocks__/**'],
+      reporter: ['text', 'html'],
+      thresholds: {
+        lines: 80,
+        functions: 80,
+        branches: 70,
+        statements: 80
+      }
+    }
+  }
+});
